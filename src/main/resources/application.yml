spring:
  application:
    name: wiseai-assignment

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/reservation_db?characterEncoding=UTF-8
    username: application
    password: application

  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    properties:
      hibernate.format_sql: true
    show-sql: true

  sql:
    init:
      mode: always

